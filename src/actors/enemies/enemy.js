import { Player } from "../character.js";

// export class Enemy extends Player{
//     constructor(name = "undefined") {
//         super(name);
//     }
//     update(){
//         this.x += (Math.random() + -Math.random()) * 5;
//         this.y += (Math.random() + -Math.random()) * 5;
//     }
// }